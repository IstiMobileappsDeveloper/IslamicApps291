<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.islamicapps">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".prayertimes.AppController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Doya_Dorud_Activity"></activity>
        <activity android:name=".ReadQuranActivity" />
        <activity android:name=".prayertimes.PrayerTimesActivity" />
        <activity android:name=".DoyaDorudActivity" />
        <activity android:name=".KiblaActivity" />
        <activity android:name=".TasbeehActivity" />
        <activity android:name=".quran.SuraDetailsActivity" />
        <activity android:name=".quran.QuranActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>